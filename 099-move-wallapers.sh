
# cd into the scripts folder
cd apt-install-scripts

sudo mv *.png /usr/share/backgrounds
sudo mv *.jpg /usr/share/backgrounds

# make a folder in the Pictures folder called LDWP
#mkdir ~/Pictures/LDWP

# move all .PNG images to LDWP
#sudo mv *.png ~/Pictures/LDWP/

# cd into LDWP (where we just sent the pictures)
#cd ~/Pictures/LDWP


# copy all proper background images to the backgrounds directory without the templates (they are still available to edit)
#sudo cp LDWP* /usr/share/backgrounds

# cd back into the script folder to run the other scripts 
#cd ~/apt-install-scripts

exit
